<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.study.koreait.mapper.PostMapper">
    <!--  ResultMap  -->
    <!--  SELECT 결과를 자바의 어떤 타입으로 매칭할건지 정의하는 것  -->
    <resultMap id="postMap" type="Post">
        <!--    pk 매핑    -->
        <id property="postId" column="post_id" />
        <!--    기본필드 매핑    -->
        <result property="title" column="title" />
        <result property="content" column="content" />
<!--        <result property="createAt" column="create_at" />-->
        <!--    1:N 관계 매핑    -->
        <collection property="comments" resultMap="commentsMap" javaType="List" />
    </resultMap>
    <resultMap id="commentsMap" type="Comments">
        <id property="commentId" column="comment_id" />
        <result property="author" column="author" />
        <result property="body" column="body" />
<!--        <result property="createAt" column="create_at" />-->
    </resultMap>
    
    <!--  SQL  -->
    <!--  id: 추상메서드 이름을 매핑해줌  -->
    <select
            id="findAll"
            resultType="Post"
    >
        SELECT * FROM post
    </select>

    <!--  LEFT 조인할 때 comments가 없는 post는 빈 리스트를 만들어옴  -->
    <select
            id="findAllPostsWithComments"
            resultMap="postMap"
    >
        SELECT p.post_id, p.title, p.content, p.create_at, c.comment_id, c.author, c.body
        FROM post p
        LEFT JOIN comments c ON p.post_id = c.post_id
        ORDER BY p.post_id, c.comment_id
    </select>


</mapper>